#ifndef __PLAYER_H__
#define __PLAYER_H__

#include "entity.h"


/**
* @brief creates a new player
* @return  returns a pointer to the new player returns null on failure
*/
Entity* player_new();
#endif